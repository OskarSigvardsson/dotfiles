alias map="xargs -n1"

alias timer='echo "Timer started. Stop with Ctrl-D." && echo && date && time cat && date'

# git aliases
alias gs="git status"
alias gc="git commit"
alias gd="git diff"
alias gls="git ls"
